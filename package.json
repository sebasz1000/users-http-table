{
  "name": "07-http-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server server/db.json --port 3001"
  },
  "devDependencies": {
    "json-server": "^0.17.3",
    "standard": "^17.0.0",
    "vite": "^4.2.0"
  },
  "eslintConfig": {
    "extends": "standard"
  }
}